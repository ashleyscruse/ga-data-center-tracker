# Local-only folders (admin docs, planning notes, contracts)
_admin/
_planning/

# Local-only files (Claude context, personal notes)
CLAUDE.md
# Obsidian KB node for this project (holds budget, internal tasks)
ga-data-center-tracker.md

# Internal meeting documents (agendas, etc.) — never publish
*.pdf

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv/
.venv-test/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data files
# Raw and interim are local-only (scraped/intermediate, often large or licensing-bound).
data/raw/
data/interim/
*.csv
*.tsv
*.parquet
*.xlsx
*.zip
*.gz

# Committed data: the county reference table and the final processed dataset are
# part of the public deliverable, so re-include them despite the broad rules above.
!data/reference/
!data/reference/**
!data/processed/
!data/processed/**

# Environment variables / secrets
.env
.env.local
*.key
credentials.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
